const config = {
  theme: {
    extend: {
      fontFamily: {
        'closnes': ['"Closeness Fonts"', 'sans-serif'],
        'deguland': ['"Deguland Fonts"', 'sans-serif'],
        'papernots':['"Papernotes Fonts"', 'sans-serif']
      },
    },
  },
  plugins: ["@tailwindcss/postcss"],
};

export default config;
